services:
  - type: web
    name: nello-downloader
    runtime: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: DOWNLOADER_TOKEN
        generateValue: true
      - key: PYTHONUNBUFFERED
        value: "1"
